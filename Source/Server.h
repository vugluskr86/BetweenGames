#pragma once

#include <memory>

namespace BWG {
   namespace Net {      
      class Server
      {
         struct Client_t {

         };
      public:
 //      void OnClientConnected(std::unique_ptr<Client> client);
//       void HandleClientAction();
//       void SendChunk();
      };
   }
}